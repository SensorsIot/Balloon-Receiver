# ESP-IDF build artifacts
build/
sdkconfig.old
managed_components/
dependencies.lock

# Python
__pycache__/
*.pyc
.venv/

# Editor / OS
.vscode/
.DS_Store

# Claude runtime state inside workspace .claude/
.claude/*
# skills/ now symlinks to the Embedded-AI-Harness bind mount, not repo content
!.claude/commands/

# Claude data persistence (credentials, sessions, history)
.claude-data/
